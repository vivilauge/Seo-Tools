name: 部署到生产环境

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v3
    
    - name: 设置 Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
        cache: 'npm'
    
    - name: 安装依赖
      run: npm ci
    
    - name: 构建检查
      run: node -c server.js
    
    - name: 部署信息
      run: |
        echo "✅ 代码检查通过"
        echo "📦 准备部署到生产环境"
        echo "💡 提示：请使用 Vercel、Railway 或 Render 等平台进行实际部署"

